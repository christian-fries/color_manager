
module.tx_colormanager_admin {
  view {
    # cat=module.tx_colormanager_admin/file; type=string; label=Path to template root (BE)
    templateRootPath =
    # cat=module.tx_colormanager_admin/file; type=string; label=Path to template partials (BE)
    partialRootPath =
    # cat=module.tx_colormanager_admin/file; type=string; label=Path to template layouts (BE)
    layoutRootPath =
  }
  persistence {
    # cat=module.tx_colormanager_admin//a; type=string; label=Default storage PID
    storagePid =
  }
}
